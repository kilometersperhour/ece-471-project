int sensorValue[9]; // photoresistor 0 and photoresistor 1 inputs
int topLeft[2]; /
int topCenter[2];
int topRight[2];
int midLeft[2];
int midCenter[2];
int midRight[2];
int botLeft[2];
int botCenter[2];
int botRight[2];

void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
  /* 
   *  Serial interface over bluetooth?
   *  Rotate slightly to 
   *  Poll photoresistors
   *    ioctl adc channel 0 annd 1 over adc
   *    capture sensor readings
   *  Compare sensor readings 
   */
}
